import { FilterCardListPipe } from './filter-card-list.pipe';

describe('FilterCardListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCardListPipe();
    expect(pipe).toBeTruthy();
  });
});
